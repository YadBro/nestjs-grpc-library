import { BookFilterExceptionFilter } from './book-filter-exception.filter';

describe('BookFilterExceptionFilter', () => {
  it('should be defined', () => {
    expect(new BookFilterExceptionFilter()).toBeDefined();
  });
});
